<div class="row standalone">
    <div class="standalone-content">
        <h2>{{title}}</h2>
        {{#each task in tasks}}
            <p>TASK: 
                {{task.description}}
            </p>
        {{/each}}
    </div>
</div>
