<div class="row full-width standalone">
    <div id="kanban-board" class='standalone-content'>
        <h2>{{meta.name}}</h2>
        {{#each column in columns}}
            {{kanban-column tasks=tasks column=column allColumns=meta.columns}}
        {{/each}}
    </div>
</div>
